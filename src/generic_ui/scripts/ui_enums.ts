module UI {
  /**
   * Enumeration of mutually-exclusive view states.
   */
  export enum View {
    SPLASH = 0,
    ROSTER,
    USER,
    NETWORKS,
    SETTINGS,
  }

  /**
   * Enumeration of mutually-exclusive UI modes.
   */
  export enum Mode {
    GET = 0,
    SHARE
  }
}


