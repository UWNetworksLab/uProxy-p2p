UI isolation folder.

Browser-specific dependencies and background scripts are mocked such that the
user interface can be interacted with as a static page.

To see the UI, open up ./view.html directly in the browser.
